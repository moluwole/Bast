os: linux
sudo: false
language: python

install:
  - pip install tornado
  - pip install bcrypt
  - pip install jinja2

branches:
  only:
    - master
    - develop

script:
  - python test/test_controller.py

notifications:
  email: false